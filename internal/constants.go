package internal

// DataDir is the directory where the raw Spotify listening data is stored.
const DataDir string = "data"

// AggregatedTrackDataFile is the name of the file where listening data aggregated by track is stored.
const AggregatedTrackDataFile string = "aggregated_tracks.json"

// AggregatedArtistDataFile is the name of the file where listening data aggregated by artist is stored.
const AggregatedArtistDataFile string = "aggregated_artists.json"
